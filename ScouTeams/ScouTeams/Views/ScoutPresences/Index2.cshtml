@model IEnumerable<ScouTeams.Models.ScoutPresence>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ScoutId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Presence)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Meeting)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ScoutId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Presence)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Meeting.MeetingId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ScoutPresenceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ScoutPresenceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ScoutPresenceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
