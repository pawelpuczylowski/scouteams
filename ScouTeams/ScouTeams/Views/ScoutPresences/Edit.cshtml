@model ScouTeams.Models.ScoutPresence

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>ScoutPresence</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ScoutPresenceId" />
            <div class="form-group">
                <label asp-for="ScoutId" class="control-label"></label>
                <input asp-for="ScoutId" class="form-control" />
                <span asp-validation-for="ScoutId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MeetingId" class="control-label"></label>
                <select asp-for="MeetingId" class="form-control" asp-items="ViewBag.MeetingId"></select>
                <span asp-validation-for="MeetingId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Presence" class="control-label"></label>
                <select asp-for="Presence" class="form-control"></select>
                <span asp-validation-for="Presence" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
